<style>
    @import url('https://fonts.googleapis.com/css2?family=Crimson+Text:ital@0;1&family=UnifrakturMaguntia&display=swap');

    :root {
        --blessed-blue: #4a90e2;
        --cursed-red: #8b0000;
        --gold: #ffd700;
        --dark-bg: #1a1a1a;
    }

    .mystical-background {
        position: fixed;
        top: 0;
        left: 0;
        width: 100vw;
        height: 100vh;
        background: radial-gradient(circle at 50% 50%, 
            rgba(20,20,40,0.95) 0%, 
            rgba(10,10,20,0.98) 100%
        );
        z-index: -2;
    }
      .shop-container {
          max-width: 1400px;
          margin: 2em auto;
          padding: 2rem;
          position: relative;
          background: linear-gradient(
              135deg, 
              rgba(10,10,20,0.98) 0%, 
              rgba(20,20,40,0.95) 50%, 
              rgba(10,10,20,0.98) 100%
          );
          box-shadow: 
              0 0 50px rgba(0,0,0,0.5),
              inset 0 0 30px rgba(139, 0, 0, 0.2),
              0 0 15px rgba(74, 144, 226, 0.1);
          border: 1px solid rgba(139, 0, 0, 0.3);
          border-radius: 8px;
          backdrop-filter: blur(10px);
          clip-path: polygon(
              0 0,
              100% 0,
              98% 2%,
              98% 98%,
              96% 100%,
              0 100%
          );
      }

      .shop-container::before {
          content: '';
          position: absolute;
          top: 0;
          left: 0;
          right: 0;
          bottom: 0;
          background: linear-gradient(45deg, transparent 30%, rgba(139, 0, 0, 0.1) 50%, transparent 70%);
          animation: shimmer 3s infinite linear;
          pointer-events: none;
      }

      @keyframes shimmer {
          0% { transform: translateX(-100%); }
          100% { transform: translateX(100%); }
      }
    .recommendations-section {
        background: rgba(10, 10, 10, 0.95);
        padding: 2em;
        margin-bottom: 3em;
        border: 1px solid #333;
        position: relative;
        overflow: hidden;
    }

    .recommendations-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5em;
    }

    .mystical-title {
        font-family: 'UnifrakturMaguntia', cursive;
        text-align: center;
        font-size: 2.5em;
        margin-bottom: 1em;
        position: relative;
    }

    .divine-realm .mystical-title {
        color: var(--blessed-blue);
        text-shadow: 0 0 15px rgba(74, 144, 226, 0.3),
                     0 0 30px rgba(74, 144, 226, 0.2);
    }

    .shadow-realm .mystical-title {
        color: var(--cursed-red);
        text-shadow: 0 0 15px rgba(139, 0, 0, 0.3),
                     0 0 30px rgba(139, 0, 0, 0.2);
    }

    .inventory-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
        gap: 2em;
        padding: 2em;
    }

    .realm-section {
        margin: 4em 0;
        position: relative;
    }

    .realm-divider {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 1em;
        margin: 2em 0;
    }

    .divider-line {
        height: 1px;
        width: 200px;
        background: linear-gradient(90deg, 
            transparent, 
            currentColor,
            transparent
        );
    }

    .divider-symbol {
        font-size: 1.5em;
        opacity: 0.7;
    }

    .quantum-deals-section {
        background: linear-gradient(135deg, rgba(20,20,20,0.98), rgba(30,20,20,0.95));
        border: 2px solid var(--cursed-red);
        box-shadow: 0 0 30px rgba(139, 0, 0, 0.2);
        padding: 3rem;
        margin: 2rem 0;
        position: relative;
        clip-path: polygon(0 0, 100% 0, 98% 100%, 2% 100%);
        z-index: 2;
    }

    .quantum-deals-button {
        font-family: 'UnifrakturMaguntia', cursive;
        font-size: 1.8rem;
        background: linear-gradient(45deg, var(--cursed-red), #660000);
        border: 1px solid rgba(255, 215, 0, 0.3);
        padding: 1.5rem;
        width: 100%;
        color: var(--gold);
        text-shadow: 0 0 10px rgba(255, 215, 0, 0.3);
        cursor: pointer;
        transition: all 0.4s ease;
    }

    .quantum-deals-button:hover {
        transform: translateY(-2px);
        box-shadow: 0 0 20px rgba(139, 0, 0, 0.4);
    }
</style>
